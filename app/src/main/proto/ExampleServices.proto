syntax = "proto3";

package com.wingoku.grpc;

option java_multiple_files = true;

message Response {
    string responseMessage = 1;
}

message Request {
    string requestMessage = 1;
}

service WingokuService {
    rpc messages(stream Request) returns (stream Response);
}
